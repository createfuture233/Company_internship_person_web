# Public assets

这个目录用于存放会直接暴露给浏览器访问的静态资源。

- `icons/`：浏览器图标、站点图标、PWA manifest 等。
- `images/`：后续文章、作品、页面中使用的普通图片。
- `illustrations/`：后续网站插画、装饰图形、主题视觉素材。

浏览器图标当前使用：

- `/assets/icons/personal-planet-favicon.svg`
